# Neko Draw 架构

插件按“平台适配、业务服务、数据存储、管理界面”四层组织，配置保存时采用完整运行时替换，避免只更新界面而实际绘图仍沿用旧设置。

## 平台适配层

- `main.py`：AstrBot 插件入口，只负责识别 `nd`、`nde`、`apng`、`nc` 指令，将 QQ 当前消息、回复与合并转发转换成业务输入，并把结果交回 AstrBot。
- `webui/__init__.py`：页面桥接 API，负责配置、提供商测试、模型测试、上传文件以及两类历史记录的管理。

## 业务服务层

- `core/runtime.py`：统一构建并原子替换所有配置驱动服务。保存配置后，模型提供商、模型路由、提示词、白名单、代理下载、发送策略和限流规则一起生效。
- `core/handler.py`：文生图与图片编辑的模型选择、参数组装和生成流程。
- `core/providers.py`：插件自己的模型提供商、AstrBot 已有提供商适配和连接测试。
- `core/output.py`：原图/APNG 包装、金句外显、合并转发和失败回退。
- `core/image_extract.py`、`core/forward_images.py`：当前消息、回复、文件和合并转发聊天记录取图。
- `core/parser.py`、`core/prompt_manager.py`、`core/templates.py`：指令参数、预设提示词与模型配置。

## 数据层

- `core/history.py`：AI 生图记录及输入原图、生成图、缩略图的统一生命周期和容量上限。
- `core/apng_history.py`：仅保留 APNG 输入原图与缩略图；成品发送后始终清理。
- `core/storage.py`、`core/uploads.py`：安全、原子的配置数据及上传文件处理。

## 配置生效规则

1. WebUI 在本地编辑草稿，不会边输入边改运行中的插件。
2. 点击右下角“保存并重载”后，后端先校验并持久化完整配置。
3. 新运行时全部创建成功后一次性接管请求；旧网络连接随后关闭。
4. 限流规则立即更新，但已消耗配额和正在执行的请求不会因保存配置而清零。
5. 若新运行时构建失败，旧运行时不会被部分覆盖。
