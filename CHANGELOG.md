# v2.2.6

- 修复回复图片后直接发送普通文本 `apng` 无响应的问题。
- APNG 改为唯一的全消息入口，统一兼容 `apng`、`/apng`、回复图片与 @机器人，避免双处理器重复执行。

# v2.2.5

- 修复 `apng` 同时经过专用命令与通用消息监听而被执行两次、重复回复的问题。
- APNG 输入在落地后按内容识别补充解析器产生的重复项，避免一张引用图被记录和制作成两帧。
- 单图 APNG 插入的配置首帧不再计入历史“输入原图”；动画仍为两帧，历史只展示用户实际提交的一张原图。
- 用户在消息链中明确发送的多张相同图片仍作为独立帧保留，不缩减原有 APNG 能力。

# v2.2.4

- 修复同一张回复图片同时被消息链与 AstrBot 引用解析器识别时，因临时 URL 不同而重复上传、历史显示两张的问题。
- 参考图落地后按文件内容哈希去重；模型请求与生图历史只保留唯一原图，并清理多余临时副本。
- APNG 帧序列不应用该去重规则，允许有意重复帧，不改变现有动画能力。

# v2.2.3

- 对照 v1.9.1 复查原图链路，并接入 AstrBot 4.28 自带的引用消息图片解析器：Reply 未携带完整图片链时，会按 reply id 重新读取原消息。
- 支持 AstrBot 标准的 `base64://` 图片引用，并继续保留直接图片、回复图片、QQ file ID 和合并转发的多级回退。
- 模型推荐参数不再自动写入；只有点击“添加推荐参数”才补充当前缺少的字段，不覆盖用户已有参数。
- 保存时自动校验两个默认模型；提供商或模型已删除、停用、禁止作为默认项、用途不匹配时，持久化为“无”。

# v2.2.2

- 修复历史原图持久化改造后 QQ 临时图片 URL 失效会导致编辑请求无法取图的问题：保留同一 Image 组件的 URL、本地路径和 file ID 候选，下载失败后调用 OneBot `get_image` / `get_file` 重新解析。
- 生图编辑和 APNG 统一使用同一个消息图片落地模块，避免两条链路再次出现不同读取结果。
- 修复 QQ URL 中 HTML 转义参数导致签名地址无效的问题，并保证通过 file ID 临时落地的文件在请求结束后清理。

# v2.2.1

- 修复把任意 `/api/v3` 地址误判为 WaveSpeed 的问题；现在只有地址明确包含 `wavespeed` 才会把模型 ID 拼接到请求路径，火山方舟等地址按 OpenAI 兼容接口处理。
- 默认文生图和默认图片编辑下拉框均新增“无”，留空后不会擅自回退到其他模型。
- 修复 SiliconFlow `Qwen/Qwen-Image-Edit` 系列：统一使用 `images/generations`，并按接口要求以单数 `image` 字段发送一张 Base64 原图；旧版错误配置也会在请求兼容层自动纠正。
- 增强 QQ 原图读取：优先使用可读本地文件，并为临时网络图片增加重定向、浏览器标识、实际图片格式探测和明确日志。
- 把协议识别、模型参数档案、服务商请求适配分别拆到独立模块，减少界面、路由和 HTTP 请求间的重复判断。

# v2.2.0

- 新增主流图片模型参数识别：首次添加 GPT Image、DALL·E、Qwen Image、Seedream、FLUX、Stable Diffusion、Imagen / Gemini Image 等模型时，自动生成对应的推荐请求参数。
- 推荐参数只在首次添加时填入，不会覆盖已经保存的人工修改；模型设置中新增“恢复推荐参数”按钮。
- 请求参数仍可逐项增加、删除和修改，QQ 指令中的同名参数继续拥有最高优先级。
- 图片编辑模型默认采用 `images/edits`，文生图模型采用 `images/generations`。

# v2.1.1

- 从 AstrBot 原生配置结构中彻底移除旧版 `model_providers`、`model_templates` 和旧默认模型字段，避免全新安装仍渲染三张空白旧提供商卡片。
- 旧字段仅作为运行时迁移输入继续兼容，不再显示在配置页面；全新安装只保留空的新版“模型提供商与模型”集合。

# v2.1.0

- 生图与 APNG 的内置开始提示统一为“小猫正在搓屏幕中/ᐠ - ˕ -マ Ⳋ📱”。
- 生图任务超时时统一提示“小猫/ᐠ - ˕ -マ Ⳋ提醒你”。
- 自动迁移仍使用旧内置提示文案的配置，自定义提示文案保持不变。
- 取消“图像接口协议”配置，连接测试、模型读取和实际生图统一根据 API Base URL 自动识别 WaveSpeed、RunningHub 或 OpenAI 兼容协议。
- WaveSpeed 提供商不再尝试读取不存在的通用模型列表，界面改为测试连接，并明确要求通过“自定义模型”添加模型 ID。
- 首次安装不再预置 WaveSpeed、RunningHub、OpenAI 提供商或任何模型，提供商与默认模型初始均为空；升级配置继续保留并迁移。

# v2.0.0

- 重构生图与 APNG 历史资产生命周期：记录、输入原图、生成图和缩略图统一归属，删除记录或超出上限时完整清理。
- 生图历史支持多张输入原图与多张生成图的缩略图、按需原图查看和逐图下载；列表不再直接读取大图。
- APNG 历史仅保存多张输入原图和缩略图；APNG 成品在消息发送完成后始终删除，不提供成品预览或下载。
- 生图与 APNG 分别新增完全无痕清理开关和独立历史保存上限；无痕模式不写数据库记录，也不保留任何插件托管文件。
- 保存配置时立即执行新历史上限；设为 0 会清空对应历史及其全部托管资产。
- 新增历史资产子表并自动兼容旧版生图历史；升级时清理旧版遗留的 APNG 成品文件。

# v1.9.1

- 右侧配置操作改为两个独立的 48px 圆角图标按钮，竖向排列并与页面右边保留间距：上方重新加载，下方保存并重载。
- 生图首帧包装产生的临时 APNG 在发送完成后自动清理；生成历史始终保留、查看和下载包装前的原始生成图。
- APNG 指令制作的作品及其独立历史不受临时包装清理影响，仍按 APNG 设置中的清理选项管理。

# v1.9.0

- 将绿色“森林实验室”写为 CSS 根级真实默认值，并为页面 CSS/JS 增加版本缓存标识，修复标签为绿色但页面仍显示旧黄色主题的问题。
- 主题切换改为静默即时切换，不再弹出“仅在本次打开期间生效”的提示。
- “重新加载”和“保存并重载”移至页面右下方悬浮操作区；桌面与手机布局均保持可触达且不占侧栏空间。
- 新增统一运行时容器，模型提供商、模型路由、提示词、白名单、代理下载、输出策略和限流规则在保存后完整、原子地刷新。
- 修复保存配置后限流开关与规则未随运行时更新的问题，并在重载时保留已使用配额和执行中的预约。
- 补充架构说明与运行时回归测试；原有生图、图片编辑、QQ 合并转发取图、APNG、默认首帧、两类历史及管理指令全部保留。

# v1.8.1

- 主导航调整为“模型提供商、插件配置、生成历史、APNG 作品”，移除独立页面设置。
- WebUI 每次打开都直接使用绿色主题，不再读取或保存主题；顶部仍可临时切换本次会话配色。
- 重新加载与“保存并重载”固定到侧栏底部，提供商页和插件配置页不再重复占用顶部空间。
- 收窄主侧栏和提供商源栏，释放更多模型配置空间。
- 修复获取模型后搜索条件没有立即作用于新列表的问题，并增加无结果提示。
- 已从服务商列表选择的模型在设置中显示固定 ID，不再出现重复选择框；自定义模型仍可手填。
- 加强提供商和单模型连接测试的成功/失败状态显示。

# v1.8.0

- 模型提供商从“插件配置”移到与生成历史并列的独立页面，桌面端使用左右分栏，手机端自动改为单列。
- 修复模型搜索仅过滤已配置模型的问题；现在同时搜索已配置模型和服务商返回的待添加模型。
- 服务商返回的模型只能从列表选择；只有“自定义模型”入口允许手工填写模型 ID。
- 默认界面改为绿色“森林实验室”，取消即时主题预览；保存后的主题会在样式加载前应用，避免页面闪色。
- 兼容 SiliconFlow 官方 `images[].url` 生图响应，并增强重定向、二进制图片类型识别、代理和下载日志。
- WebUI 保存后会完整刷新提供商、模型路由、提示词、限流、下载器和发送设置，无需重启插件。

# v1.7.1

- 模型提供商页改为与 AstrBot 相同的左右分栏：左侧提供商源，右侧直接编辑连接、高级配置与模型列表，不再使用提供商弹窗。
- 模型行支持就地启用/停用、连接检查、详细参数设置和删除；支持搜索、获取模型列表与自定义模型。
- 插件模型提供商只管理插件自己的 URL、Key 和协议，不再出现 AstrBot 提供商引用选项。
- AstrBot 已有模型仅出现在“默认模型选择”中，继续支持直接选作默认文生图或默认图片编辑模型。
- 超时时间、代理地址和自定义请求头支持按插件提供商独立设置。

# v1.7.0

- 从数据结构开始重构模型配置：模型与独立参数现在直接归属于模型提供商，不再维护互相引用的“提供商列表 + 模型模板列表”。
- 提供商连接测试成功后会读取并展示服务商公开的模型列表，可直接选择模型，也可继续添加服务商未公开列出的自定义模型。
- 模型设置保留用途、回退优先级、参考图字段、数量限制、提示词限制和自由请求参数；OpenAI 兼容模型自动使用 `images/generations` 请求端点。
- 每个已配置模型新增独立连接检查，结果直接显示在弹窗内，不再被弹窗遮罩挡住。
- 默认模型独立为“默认文生图”和“默认图片编辑”，同时支持插件内启用模型及 AstrBot 已有模型。
- 保存配置后会立即重建模型路由和 API 客户端，新 URL/Key 无需重启插件即可生效。
- 自动迁移 v1.6.0 及更早版本的提供商与模板配置，用户无需重新录入。

# v1.6.0

- `apng` 现在可以不填写间隔参数，直接发送时使用 WebUI 中可配置的默认值，默认 5 秒。
- APNG 循环次数增加 QQ 手机端可能始终无限循环、忽略文件循环次数的说明。
- 默认文生图和图片编辑模型改为从可用模型模板中下拉选择。
- 模型提供商卡片新增不产生生图任务的连接测试。
- 模型提供商支持引用 AstrBot 已有提供商，不重复保存其 API Key；需要该提供商具备 OpenAI 兼容图像端点。
- 默认回退优先级现在会在指定默认模板不可用时生效，数字越小优先级越高。
- 已保存的 WebUI 主题会在样式加载前通过本地缓存恢复，避免打开页面时先闪过其他配色。
- 默认开始画图提示改为“小猫正在搓屏幕中”。

# v1.5.0

- 修复 Jinja2 模板遗漏 APNG 作品页面导致初始化脚本中断、状态永久停留在“正在连接”的问题。
- 移除重复加载的桥接 SDK，使用 AstrBot 自动注入对象；增加桥接超时和初始化异常兜底。
- 左侧主导航新增独立“页面设置”，界面主题不再混入插件业务配置分类。
- 完善手机布局：抽屉侧栏与遮罩、记录卡片化、单列配置表单、移动端弹窗与操作按钮适配。

# v1.4.3

- WebUI 新增“界面设置”分类，主题改为统一走“有未保存的修改 → 保存配置”流程，不再点击即单独保存。
- 顶部主题选择器与界面设置字段同步，未保存的主题预览不会覆盖服务器上的已保存选择。

# v1.4.2

- WebUI 主题改为保存到插件配置，重新打开页面后自动恢复上次选择。
- `apng` 新增可选的 `--fd` 首帧时长参数；未填写时使用 APNG 设置中的默认首帧时长，单图和多图均生效。

# v1.4.1

- 修复用户在页面桥接尚未就绪时进入配置页导致的“页面桥接未连接”；请求现在会等待桥接完成，并显式加载官方桥接 SDK。
- APNG 设置新增独立的生成中提示开关和提示文案。

# v1.4.0

- WebUI 将发送配置拆分为互不影响的“生图设置”和“APNG 设置”。
- 生图与 APNG 可分别配置图片外显金句、金句库、合并转发、转发昵称、@ 触发者和默认首帧。
- APNG 指令未填写循环次数时，使用 WebUI 中配置的默认循环次数。

# v1.3.2

- 修复 Python 3.10–3.13 加载插件时，`HistoryStore.list` 遮蔽内置 `list` 类型而触发的 `'function' object is not subscriptable`。

# v1.3.1

- 修复 `/nc` 指令组占位函数签名不符合 AstrBot 规范导致插件加载失败。
- WebUI 配置侧栏新增独立“APNG 设置”，从“发送增强”中完整拆分 APNG 配置。

# v1.3.0

- 支持读取 QQ/OneBot 合并转发聊天记录中的图片，包括回复转发卡片的场景。
- APNG 最低图片数可配置；设为 1 时自动补入当前默认首帧。
- 新增独立“APNG 作品”页面，可预览、下载、逐条删除或清空服务器文件。
- 新增 APNG 发送后立即清理开关；开启后不保留文件和制作记录。
- 图片编辑历史详情新增原图预览，并自动迁移旧版历史数据库。

# v1.2.0

- 新增 `apng <间隔秒数> [循环次数]` 多图 APNG 制作，支持 2～20 张图片。
- 新增 `/nc` 管理指令组，并将所有插件指令统一为字母缩写。
- 新增 `/nc fs`、`/nc fv`、`/nc fr`，可在 QQ 设置、查看和恢复 APNG 首帧。
- 默认绘图与编辑触发词改为 `nd`、`nde`。

# v1.1.0

- API 密钥配置改为可添加的“模型提供商”卡片。
- 模型提供商支持自定义唯一名称，并选择 WaveSpeed、RunningHub 或 OpenAI 类型。
- 模型模板改为按名称选择模型提供商。
- 增加提供商唯一性、类型和模型引用校验。

# v1.0.0

- 首次发布“猫娘画图”。
- 支持猫娘首帧与生成结果的 APNG 双帧展示。
- 支持 WaveSpeed、RunningHub 与 OpenAI 兼容图片接口。
- 支持文生图、参考图编辑、模型模板和预设提示词。
- 提供多主题 WebUI、生成历史、图片下载、限流和白名单管理。
